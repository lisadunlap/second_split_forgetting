seed: 0
exp:
  run: 'sub'
  num_epochs: 200
  debug: false
  wandb: true

model:
  resume: false

data:
  dataset: 'CubDirty'
  train_first_split: 'even'
  test_dataset: 'CubDirty'
  batch_size: 256
  root: '/shared/lisabdunlap/data'

hps:
  lr: 0.001
  weight_decay: 5e-5

noise:
  p: 0.00
  method: noop
  # clean_val: false